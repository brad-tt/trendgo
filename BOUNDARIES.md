# TrendGo Boundaries

本文件定义当前产品边界。判断冲突时，以这里、`README.md`、`app/README.md` 和代码当前行为为准。

## 数据边界

- SQLite `app/database/toptrader.db` 是交易数据真相来源。
- Markdown 记录是归档和兼容副本，不是第二套主数据库。
- 前端状态不能成为交易事实来源。
- 回归脚本必须使用沙盒保护，不能污染真实交易日。
- 删除历史交易、批量改库、迁移 schema 前必须确认。

## Agent 边界

Agent 可以：

- 解析自然语言。
- 识别 `open_trade` / `close_trade` / `rule_event` / `review`。
- 整理结构化字段。
- 缺字段时追问。
- 字段齐全时调用结构化 CLI 写入。
- 展示规则风险、知识路由和写入结果。

Agent 不可以：

- 编造价格、仓位、止损、标的代码、交易编号。
- 把缺字段 proposal 当成完成。
- 把闲聊直接写成交易记录。
- 自动修改规则原文或知识路由。
- 自动删除历史交易。
- 把疑似失真定性为最终结论。

## 后端边界

- `/api/agent/commit` 不做自然语言解析。
- 后端必须保留字段校验、枚举规范化、时间/数字处理和盈亏计算。
- 开仓、纪律事件、复盘可以自动创建目标工作台。
- 平仓不能自动创建空工作台。
- 后端必须返回明确的 `committed`、`missingFields` 和 `message`。

## 前端边界

- 当前前端仍是工作台，不是纯仪表盘。
- 表单入口保留，用于纠错和补录。
- Agent 面板应服务于字段确认、缺失项、规则提示和写入结果。
- 不为了视觉简化隐藏关键字段、规则事件或写入状态。

## 规则和知识边界

- `trading-system/00-index/rules-catalog-v1.md` 是 Agent 规则引用索引，不替代规则原文。
- `toptrader-research/00-index/knowledge-router-v1.md` 是知识路由索引，不替代 playbook。
- 新增规则或路由必须有来源文件和编号。
- 未确认的新规则只能作为提案。

## 当前不做

- 不新增独立 NLP 服务。
- 不新增第四套平行系统。
- 不做自动交易。
- 不做多用户体系。
- 不做复杂长期画像。
- 不做完整能力评分。
- 不做全量知识库搜索产品。
